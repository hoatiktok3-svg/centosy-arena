# Commit Checkpoint Guide

Sau mỗi step:
1. Chạy build/test.
2. Kiểm tra git diff.
3. Commit riêng từng step.

```bash
git add .
git commit -m "feat: STEP [SỐ] [short-feature-name]"
```

Nếu chỉ tạo docs:

```bash
git add .
git commit -m "docs: update CENTOSY ARENA roadmap package"
```
